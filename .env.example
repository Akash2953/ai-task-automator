DATABASE_URL="file:./packages/db/dev.db"

# Optional — enables LLM natural-language parsing. Without it, heuristic parser is used.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Worker poll interval in milliseconds
WORKER_POLL_MS=15000
